// Prelude; io library in std standard library
use std::io;

fn main() {
    println!("Guess the number!");
    println!("Please input your guess.");
    
    // Use "let" to make variables
    // 
    // In Rust, Variables are immutable by default,
    // But Can be overriden by using "mut" keyword.
    //
    let mut guess = String::new(); 
    // At String::~, :: indicates Associated Function.
    // As a result, line 13 creates new empty String 
    // instance and mutable variable connected.

    // Now the code calls "stdin" associated function of
    // "io" and this will make the code able to handle
    // user input and output.
    //
    // Even if we didn't import io library using use key
    // at line 2, We can use this function as we import
    // the function by using "std::io::stdin" at the call
    // point of function.
    //
    // The stdin Function returns an instance of "std::io::Stdin".
    io::stdin()
        // Now the program calls "read_line" method at stdio,
        .read_line(&mut guess)
        .expect("Failed to read line");

    println!("You guessed: {guess}");
}
